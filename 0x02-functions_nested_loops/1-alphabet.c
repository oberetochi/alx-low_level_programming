#include "main.h"

/**
 * main - entry point
 *
 * description: Using custom header to print alphabets
 * *Return: 0 if successful
 */

int main(void)
{
	char alpha = a;

	while (alpha <= z)
	{
		_putchar(a);
		alpha++;
	}
	return (0);
}

