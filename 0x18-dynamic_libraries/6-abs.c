#include "main.h"
/**
 * _abs - check the absolute value of an integer.
 * @n: A integer number
 *
 * Return: n if is positive,
 * Otherwise, return -n
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return ((-1) * n);
}
